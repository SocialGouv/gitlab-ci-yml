#

include:
  - /base_autodevops.yml

#

Install:
  extends:
    - .autodevops_install
Build:
  extends:
    - .autodevops_build
Lint:
  extends:
    - .autodevops_lint
Test:
  extends:
    - .autodevops_test
Register:
  extends:
    - .autodevops_register_image
# Build:
#   extends: .autodevops_build
