#

include:
  - /base_autodevops.yml

#

Install:
  extends:
    - .autodevops_install
Build:
  extends:
    - .autodevops_build
Lint:
  extends:
    - .autodevops_lint
Test:
  extends:
    - .autodevops_test
# Build:
#   extends: .autodevops_build

# Register image:
#   extends: .autodevops_register_image
